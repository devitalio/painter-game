"use strict";

var ball = {};

ball.initialize = function () {
   
};

ball.update = function (delta) { };
ball.reset = function () { };

ball.draw = function () {
    
}

ball.handleInput = function (delta) {
    
};
